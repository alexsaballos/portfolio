---
import type { NavigationPageType } from "../../types/PagesType";
import type { AdmittedLanguagesType, LocalesType } from "../../types/LocalesType";
import { ICONS } from "../../config/assets";
import { ROUTE } from "../../config/routing";
import Icon from "../ui/libraries/Icon.astro";

const { locale, t } = Astro.locals as {
	locale: AdmittedLanguagesType;
	t: LocalesType;
};

const { page } = Astro.props as { page: NavigationPageType; };
---

<div class="breadcrumbs text-md ml-5 sm:ml-10">
	<ul>
		<li><a href={ROUTE.home(locale)} class="hover:text-secondary hover:font-bold">
			<Icon icon={ICONS.home} size="xs" />
			<p>{t.breadcrumbs.home}</p>
		</a></li>
		<li><a href={ROUTE.page(locale, page.slug)} class="hover:text-secondary hover:font-bold">
			<Icon icon={page.icon} size="xs" />
			<p>{t.breadcrumbs[page.key]}</p>
		</a></li>
	</ul>
</div>